# UI Service Environment Configuration Example
# Copy this file to ui.env and update with your actual values

# Next.js public environment variables
# The API URL that the UI will use to make requests
# For local development, this should point to the api service
# For Docker Compose, use the service name: http://api:3000/api/v1
# For production, use your production API domain
NEXT_PUBLIC_API_URL=http://api:3000/api/v1

# Server Configuration
# Port on which the UI server will listen
PORT=8080
