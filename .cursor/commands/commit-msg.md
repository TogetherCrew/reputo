# Goal: generate commit message

setps:

- Generate Conventional Commit message(s) strictly compatible with @commitlint/config-conventional.
- Base everything ONLY on the provided git diff / changed files list (no guessing).
- Identify and explicitly list affected apps/packages from names (e.x. api).
