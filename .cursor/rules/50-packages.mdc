---
description: Shared packages rules (public API, boundaries)
globs:
  - "packages/**"
---

# Package design

- Keep packages framework-agnostic unless explicitly framework-specific.
- Expose a clean public API via barrel export (`src/index.ts`); keep internals private.
- Never import from apps/.
