{
    "key": "contribution_score",
    "name": "Contribution Score",
    "category": "Activity",
    "summary": "Scores each SubID from Deep Funding Portal comments linked through a Deep Funding user ID.",
    "description": "### What it does\nScores users based on the comments they write in DeepFunding.\n\n### How it works\n- Each SubID can point to a `deepProposalPortalId` from Deep Funding Portal data.\n- Each valid comment starts with a base score.\n- Upvotes add points and downvotes remove points.\n- Comments on your own proposal or self-replies can get a penalty.\n- A proposal owner upvote can add a bonus.\n- Older comments can lose weight over time.\n- Only the SubIDs provided in the uploaded JSON are scored.\n\n### Data source\nUses proposal, comment, user, and vote data from DeepFunding together with the uploaded SubID JSON. For this algorithm, only `deepProposalPortalId` is used. All JSON fields are optional and may be empty.\n\n### Output\n- CSV file with `sub_id` and `contribution_score`.\n- JSON file with score details for comments and scored SubIDs.",
    "version": "1.0.0",
    "inputs": [
        {
            "key": "sub_ids",
            "label": "SubID Input (JSON)",
            "description": "Upload the unified SubID JSON file. Only `deepProposalPortalId` is used by this algorithm, but the full shared schema is accepted.",
            "type": "json",
            "required": true,
            "json": {
                "maxBytes": 5242880,
                "schema": "sub_id_input_map",
                "allowedChains": ["ethereum", "cardano"]
            }
        },
        {
            "key": "comment_base_score",
            "label": "Base Score Per Comment",
            "description": "Points each valid comment gets before votes, bonuses, penalties, and decay.",
            "type": "integer",
            "min": 0,
            "max": 100,
            "default": 10,
            "step": 1,
            "required": true
        },
        {
            "key": "comment_upvote_weight",
            "label": "Upvote Weight",
            "description": "How much one upvote increases a comment score.",
            "type": "number",
            "min": 0,
            "max": 10,
            "default": 1,
            "step": 0.1,
            "required": true,
            "uiHint": { "widget": "slider" }
        },
        {
            "key": "comment_downvote_weight",
            "label": "Downvote Weight",
            "description": "How much one downvote reduces a comment score.",
            "type": "number",
            "min": 0,
            "max": 10,
            "default": 1,
            "step": 0.1,
            "required": true,
            "uiHint": { "widget": "slider" }
        },
        {
            "key": "self_interaction_penalty_factor",
            "label": "Self-Interaction Penalty",
            "description": "Multiplier for comments on your own proposal or for self-replies. Use `1` for no penalty.",
            "type": "number",
            "min": 0,
            "max": 1,
            "default": 1,
            "step": 0.1,
            "uiHint": { "widget": "slider" },
            "required": true
        },
        {
            "key": "project_owner_upvote_bonus_multiplier",
            "label": "Proposal Owner Upvote Bonus",
            "description": "Multiplier when the proposal owner upvotes a comment. Use `1` for no bonus.",
            "type": "number",
            "min": 1,
            "max": 10,
            "default": 1,
            "step": 0.1,
            "required": true,
            "uiHint": { "widget": "slider" }
        },
        {
            "key": "engagement_window_months",
            "label": "Lookback Window (Months)",
            "description": "Only comments from this number of past months are scored.",
            "type": "integer",
            "min": 1,
            "max": 72,
            "default": 24,
            "step": 1,
            "required": true
        },
        {
            "key": "monthly_decay_rate_percent",
            "label": "Monthly Decay Rate (%)",
            "description": "Percent removed from a comment score for each month of age. Use `0` for no decay.",
            "type": "integer",
            "min": 0,
            "max": 100,
            "default": 0,
            "step": 1,
            "required": true
        }
    ],
    "dependencies": [{ "key": "deepfunding-portal-api" }],
    "outputs": [
        {
            "key": "contribution_score",
            "label": "Contribution Score (CSV)",
            "description": "CSV file with one row per SubID and the final contribution score.",
            "type": "csv",
            "csv": {
                "hasHeader": true,
                "delimiter": ",",
                "columns": [
                    {
                        "key": "sub_id",
                        "type": "string",
                        "description": "SubID from the uploaded JSON input."
                    },
                    {
                        "key": "contribution_score",
                        "type": "number",
                        "description": "Final contribution score for the SubID."
                    }
                ]
            }
        },
        {
            "key": "contribution_score_details",
            "label": "Contribution Score Details (JSON)",
            "description": "JSON file with score details for comments and scored SubIDs.",
            "type": "json"
        }
    ],
    "runtime": "typescript"
}
