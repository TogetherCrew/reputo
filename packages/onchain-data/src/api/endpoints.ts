/**
 * API endpoint path builders for onchain data sources.
 *
 * Resource-specific endpoints will be added as resources are implemented.
 */
export const endpoints = {} as const;

export type EndpointKey = keyof typeof endpoints;
